import React from 'react-router-dom';

class User extends React.Comments {
    constructor(userId, image, name) {
        this.userId = userId;
        this.image = image;
        this.name = name;
    }
}
export default User;